# Git

Personal Git configuration.

## Local configuration

The `.gitconfig` includes `~/.gitconfig.local` for user-specific settings that should not be stored in this repository.

Create `~/.gitconfig.local`:

```ini
[user]
    name = Your Name
    email = you@example.com
```
